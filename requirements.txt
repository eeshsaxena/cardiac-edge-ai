# ── Core ML ───────────────────────────────────────────────────────────────────
numpy>=1.24.0
scipy>=1.11.0
scikit-learn>=1.3.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0

# ── Deep Learning ─────────────────────────────────────────────────────────────
tensorflow>=2.16.0          # Keras 3 backend
keras>=3.0.0

# PyTorch (GPU) — install separately with CUDA:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
# torch>=2.2.0
# torchvision>=0.17.0

# ── Signal Processing ─────────────────────────────────────────────────────────
PyWavelets>=1.4.0           # wavelet decomposition for L_spectral
wfdb>=4.1.0                 # MIT-BIH / PTB-XL record reading
neurokit2>=0.2.7            # ECG quality metrics

# ── Data ──────────────────────────────────────────────────────────────────────
imbalanced-learn>=0.11.0    # SMOTE class balancing
h5py>=3.9.0

# ── Deployment / Dashboard ────────────────────────────────────────────────────
flask>=3.0.0
flask-socketio>=5.3.0
pyserial>=3.5               # Arduino serial communication
eventlet>=0.35.0            # async backend for flask-socketio

# ── Utilities ─────────────────────────────────────────────────────────────────
tqdm>=4.65.0
requests>=2.31.0
Pillow>=10.0.0
